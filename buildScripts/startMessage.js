// The following line uses the notation of ES5
//var chalk = require('chalk');
// The following line uses the notation of ES6
import chalk from 'chalk';

// The following line disables that type of warning for that specific line
console.log(chalk.green('Starting app in dev mode')); // eslint-disable-line no-console
